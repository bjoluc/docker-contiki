version: "3.3"

services:
  contiki:
    image: bjoluc/contiki:latest
    # build: .
    environment:
      - DISPLAY=$DISPLAY
    volumes:
      - "/tmp/.X11-unix:/tmp/.X11-unix"
      - "./src:/home/user/src"
    ports:
      - "2222:22"
